package handlers_test

import (
	"testing"
)

// Product handlers interact directly with the database
// Unit tests for these handlers are skipped as per requirements
func TestProductHandlers(t *testing.T) {
	t.Skip("Skipping tests for handlers that interact with the database")
}
