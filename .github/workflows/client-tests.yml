name: Client tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: clients/package-lock.json

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: clients/node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('clients/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        working-directory: clients
        run: npm ci --prefer-offline --no-audit
      - name: Lint
        working-directory: clients
        run: npm run lint
      - name: Build
        working-directory: clients
        run: npm run build
